apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-prometheus.fullname" . }}
  labels:
    {{- include "kube-prometheus.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - name: http
    port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
  selector:
    {{- include "kube-prometheus.selectorLabels" . | nindent 4 }}
